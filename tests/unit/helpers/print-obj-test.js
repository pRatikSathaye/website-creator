import { printObj } from 'website-creator/helpers/print-obj';
import { module, test } from 'qunit';

module('Unit | Helper | print obj');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = printObj([42]);
  assert.ok(result);
});
